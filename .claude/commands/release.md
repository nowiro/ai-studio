---
description: Cut a release using nx release (pre-flight + release + post-release follow-ups)
argument-hint: [optional release notes]
allowed-tools: Read, Glob, Grep, Bash, Edit, Write, Agent
---

Spawn the **release-manager** subagent and run the release flow per `.ai/agents/release-manager.md`.

Notes: $ARGUMENTS
