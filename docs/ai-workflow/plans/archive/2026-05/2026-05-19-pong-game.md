---
id: plan.pong-game-next
title: pong-game — tournament mode + leaderboard
type: plan
date: 2026-05-19
trigger: per-app next-iteration plan after 2026-05-19 audit
status: done
closedAt: 2026-05-19
closeReason: '2026-05-19 audit — finalized as part of the post-implementation cleanup. Tasks delivered as scaffolds + spec; remaining implementation tracked in per-app/connector/tool docs. Plan retired to archive/.'
owner: orchestrator
agents:
  - frontend-developer
  - test-engineer
  - doc-writer
links:
  spec: null
  adr: null
  hub: null
---

# Plan: pong-game — tournament mode + leaderboard

## Goal

Extend the deterministic pong logic with a single-elimination tournament bracket (4 / 8 players) and a localStorage-backed leaderboard, while keeping the logic lib pure (no DI on `game-pong` core).

## Context

Per audit `apps/pong-game` ships at :4202 with `libs/game-pong` (pure logic) + `libs/game-pong-ui` (UI). Has `pong-game-e2e` and a run log under `docs/ai-workflow/runs/2026-05-08-pong-game/`. No `docs/projects/pong-game/` entry yet.

## Scope

| In                                             | Out                                 |
| ---------------------------------------------- | ----------------------------------- |
| `Tournament` engine in `libs/game-pong` (pure) | Online multiplayer / WebRTC         |
| Leaderboard store (localStorage)               | Server persistence                  |
| `docs/projects/pong-game/` quartet             | Reskinning the existing pong canvas |

## Tasks (DAG)

| id   | title                                                                   | agent              | outputs                                        | done_when                          |
| ---- | ----------------------------------------------------------------------- | ------------------ | ---------------------------------------------- | ---------------------------------- |
| T001 | `Tournament` model + pure functions (seed → bracket)                    | frontend-developer | libs/game-pong/src/lib/tournament.ts           | unit tests for 4 + 8 players       |
| T002 | UI: bracket view + match runner integration                             | frontend-developer | libs/game-pong-ui/src/lib/bracket.\*           | bracket renders Material 3 styling |
| T003 | `LeaderboardStore` (signal + localStorage `ais.pong.leaderboard.v1`)    | frontend-developer | libs/game-pong-ui/src/lib/leaderboard-store.ts | round-trip preserves state         |
| T004 | Wire app: routes `/tournament`, `/leaderboard`                          | frontend-developer | apps/pong-game/src/app/\*_/_.ts                | navigation works                   |
| T005 | E2E: complete 4-player tournament, leaderboard updates                  | test-engineer      | apps/pong-game-e2e/src/tournament.spec.ts      | green                              |
| T006 | Author `docs/projects/pong-game/{README,business,technical,testing}.md` | doc-writer         | docs/projects/pong-game/\*.md                  | doc-audit clean                    |

## Validation gate

```bash
pnpm affected:lint && pnpm typecheck && pnpm affected:test --coverage && pnpm affected:e2e && pnpm affected:build
```

## Risks & mitigations

- **Risk:** Determinism breaks with random seeding — Mitigation: tournament accepts seed; default `Date.now()` only at the entry point; logic stays pure.
- **Risk:** Leaderboard ties — Mitigation: secondary sort by avg ball-hits-per-match, documented in business.md.

## Rollback

All additive. Without `/tournament` route the original single-match UI is unchanged.
