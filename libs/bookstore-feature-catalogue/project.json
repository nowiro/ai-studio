{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "bookstore-feature-catalogue",
  "$schema_comment": "Catalogue page + filter panel + book detail for the bookstore demo.",
  "projectType": "library",
  "sourceRoot": "libs/bookstore-feature-catalogue/src",
  "tags": ["scope:bookstore", "type:feature"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/bookstore-feature-catalogue/tsconfig.json --noEmit"
      }
    }
  }
}
