{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "journal-feature-attendance",
  "$schema_comment": "Attendance calendar + roll-call for school-journal.",
  "projectType": "library",
  "sourceRoot": "libs/journal-feature-attendance/src",
  "tags": ["scope:school-journal", "type:feature"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/journal-feature-attendance/tsconfig.json --noEmit"
      }
    }
  }
}
