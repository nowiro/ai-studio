{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "library-feature-catalogue",
  "metadata": { "description": "Catalogue page + book detail + search bar for the library demo." },
  "projectType": "library",
  "sourceRoot": "libs/library-feature-catalogue/src",
  "tags": ["scope:library", "type:feature"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/library-feature-catalogue/tsconfig.json --noEmit"
      }
    }
  }
}
