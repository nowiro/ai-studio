{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "library-feature-librarian",
  "$schema_comment": "Librarian view: all loans, overdue list, add book, KPI strip.",
  "projectType": "library",
  "sourceRoot": "libs/library-feature-librarian/src",
  "tags": ["scope:library", "type:feature"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/library-feature-librarian/tsconfig.json --noEmit"
      }
    }
  }
}
