/**
 * Public API for `starter-feature` — the lone feature lib for the reference
 * `apps/starter` showcase. Exposes the lazy-loadable routes; the app's root
 * `app.routes.ts` mounts them at `''`.
 *
 * @packageDocumentation
 */
export { STARTER_FEATURE_ROUTES } from './lib/routes';
export { HomeComponent } from './lib/home/home.component';
