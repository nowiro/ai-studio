/**
 * Public API for `starter-ui` — presentational components specific to the
 * starter / reference app. Used by `starter-feature` for the design-token
 * showcase. Not consumed by other apps; if a pattern repeats ≥ 3× across
 * apps, promote it to `libs/ui-kit/` per `.ai/rules/styling.md §11`.
 *
 * @packageDocumentation
 */
export { TokenCardComponent } from './lib/token-card/token-card.component';
export { TypographyRowComponent } from './lib/typography-row/typography-row.component';
