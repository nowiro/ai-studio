{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "tire-ui",
  "$schema_comment": "Presentational components for the tire-shop demo.",
  "projectType": "library",
  "sourceRoot": "libs/tire-ui/src",
  "tags": ["scope:tire-shop", "type:ui"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/tire-ui/tsconfig.json --noEmit"
      }
    }
  }
}
