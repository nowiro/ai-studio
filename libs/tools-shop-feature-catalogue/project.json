{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "tools-shop-feature-catalogue",
  "$schema_comment": "Catalogue page + filter panel + product detail for the tools-shop demo.",
  "projectType": "library",
  "sourceRoot": "libs/tools-shop-feature-catalogue/src",
  "tags": ["scope:tools-shop", "type:feature"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/tools-shop-feature-catalogue/tsconfig.json --noEmit"
      }
    }
  }
}
