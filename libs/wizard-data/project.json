{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "wizard-data",
  "projectType": "library",
  "sourceRoot": "libs/wizard-data/src",
  "tags": ["scope:wizard", "type:data-access"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/libs/wizard-data"],
      "options": {
        "command": "vitest run",
        "cwd": "libs/wizard-data"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/wizard-data/tsconfig.json --noEmit"
      }
    }
  }
}
